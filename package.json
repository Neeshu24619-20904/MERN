{
  "name": "whatsapp-clone",
  "version": "1.0.0",
  "description": "WhatsApp Clone MERN Stack",
  "main": "server/index.js",
  "scripts": {
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install && cd ../socket && npm install",
    "build": "cd client && npm run build",
    "start": "node server/index.js",
    "dev": "cd server && npm run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"
  }
}